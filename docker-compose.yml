services:
  frontend:
    build: ./frontendapp
    ports:
      - "3000:3000"
    networks:
      - mern

  backend:
    build: ./backendapp
    ports:
      - "2032:2032"
    networks:
      - mern
    # depends_on:
    #   - mongodb

  # mongodb:
  #   image: mongo:latest
  #   ports:
  #     - "27017:27017"
  #   networks:
  #     - mern
  #   volumes:
  #     - mongo-data:/data/db

networks:
  mern:
    driver: bridge

volumes:
  mongo-data:
    driver: local
